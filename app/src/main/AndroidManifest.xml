<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tcc.sicv">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.MyTicketsActivity"></activity>
        <activity android:name=".ui.TicketDetailsActivity" />
        <activity android:name=".ui.DetailsMaintenanceActivity" />
        <activity android:name=".ui.VehicleDetailsActivity" />
        <activity android:name=".ui.MaintenanceActivity" />
        <activity android:name=".ui.MyVehiclesActivity" />
        <activity android:name=".ui.BuyVehiclesActivity" />
        <activity android:name=".ui.ProfileActivity" />
        <activity android:name=".ui.HomeActivity" />
        <activity
            android:name=".ui.LoginActivity"
            android:windowSoftInputMode="stateHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.SignUpActivity"
            android:windowSoftInputMode="stateHidden" />
        <activity android:name=".base.BaseActivity" />
    </application>

</manifest>